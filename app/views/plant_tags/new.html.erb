<div class="container">
  <%= simple_form_for([@plant, @plant_tag]) do |f| %>
    <%= f.association :tag, input_html: { multiple: true, class: 'multiple-select' } %>
    <%= f.submit class: 'btn btn-primary' %>
  <% end %>
</div>
